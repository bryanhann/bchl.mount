#!/bin/bash
MOUNTPOINTS="/media/${USER} /Volumes"
main () {
    for mountpoint in ${MOUNTPOINTS}; do
        borg.ls.fullpath ${mountpoint} | while read LINE; do
            register="$LINE/.bch.mnt/register"
            [ -f "${register}" ] && "${register}"
        done
    done
}
main

